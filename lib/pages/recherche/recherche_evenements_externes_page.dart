import 'package:flutter/material.dart';

class RechercheEvenementsExternesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("recherche événements externes ;)"),
    );
  }
}
